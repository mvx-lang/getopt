CATALOG/
LIB/
bin/
mvxdata.lmdb/
BP.O/
*.dSYM/
*.o
*.dylib
*.so
BP.INC/PLATFORM.H
MVPKG.INC/PLATFORM.H

# The VOC's own dictionary is the platform's, not the account's — every
# account is born with it, and a clone gets its own (mv_git#95).
VOC.DICT/
